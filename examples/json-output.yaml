# Structured JSON output with a schema
name: json-output
steps:
  - id: extract
    pipeline: "llm.invoke --provider copilot --prompt 'List 3 popular programming languages with their year of creation. Return JSON array with name, year, paradigm fields.'"
