# Main workflow that calls a sub-workflow
name: sub-workflow-demo
description: Demonstrates calling a sub-workflow with arguments

steps:
  - id: call-greet
    workflow: "./greet.yaml"
    workflow_args:
      name: "Lobster"

  - id: show-result
    run: "echo Sub-workflow said: $call-greet.stdout"
